<div class="card centered">
    {{#imageUrl}}
    <a href="{{permalink}}">
        <img class="large rounded" src="{{imageUrl}}" alt="{{title}}">
    </a>
    {{/imageUrl}}
    <h2><a href="{{permalink}}">{{title}}</a></h2>
    <p>{{count(posts)}} articles</p>
</div>
