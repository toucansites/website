id: guide

properties:
    order:
        type: int
        required: true

relations:
    category:
        references: category
        type: one

queries:
    prev:
        contentType: guide
        scope: detail
        limit: 1
        orderBy:
            - key: order
              direction: asc
    next:
        contentType: guide
        scope: detail
        limit: 1
        orderBy:
            - key: order
              direction: asc