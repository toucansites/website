id: category
template: docs.category.default

properties:
    order:
        type: int
        required: true

queries:
    guides:
        contentType: guide
        scope: list
        limit: 10
        orderBy:
            - key: order
              direction: asc
        filter:
            key: category
            operator: equals
            value: "{{id}}"